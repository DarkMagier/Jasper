package app.jackiey.jasper.middle.passes;

/**
 * Placeholder for future pass pipeline (AST->HIR->MIR->...).
 * Kept intentionally minimal so that future refactors don't disturb CLI/backends.
 */
public final class PassManager {
    // Future: List<Pass<?, ?>> passes; run(...) with session/diagnostics
}
